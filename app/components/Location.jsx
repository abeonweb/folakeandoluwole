import React from 'react'

const Location = ({id, title, time, address, mapURL, info}) => {
  return (
    <div key={id}>
        "Location info will be right here"
    </div>
  )
}

export default Location